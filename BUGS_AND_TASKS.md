Баги (нужно дополнительно провалидировать на актуальной версии)

- У тестирования перестали нормально считаться тест поинты в ячейке исполнителя. Отображается 0
- Кнопка открытия события занятости пересекается с карточкой занятости. Можно пробовать делать ячейку с событием занятости в которой лежит задача больше по высоте, но не уверен, что это норм решение
- ~~У некоторых пользователей проблема с добавлением файлов/заметок. Ничего не добавляется пока пользователь сам не сбросит куку и локальное хранилище~~
- Перенос фичи в другой спринт удаляет все заметки, но потом они появляются в отображении по исполнителям, с привязкой к родителю, но в отображении по фичам не отображаются
- ~~Нужно убрать упоминания Booking. Да и зачем в модалке добавления команды отображать будущий слаг команды. Вообще зачем где-то отображать слаг команды~~
- ~~Стоит по-умолчанию предраскрашивать статусы по типам, сейчас inProgress тип красится в синий, а вот done нет. А еще сейчас все типы без группировки, при том, что группировка должна быть по типу. Скругления у группы списка сломаны из-за того, что он position sticky. Такая же история у футера с кнопкой сохранения~~ 
- ~~Если добавить пользователя в команду и не уходя из попапа редактирования состава попробовать удалить, то будет выведен текст "Удалить пользователя (длинний айди пользователя) вместо имени"~~
- ~~Проблемы с текстовками. Много где пишется из Трекера. Это может вызывать путаницу, так как для пользователя Трекер нарецательное = Яндекс Трекер, также как Jira. Стоит поправить текстовки чтобы они имели контекст подключенной интеграции~~
- ~~Канбан Jira. Не грузятся колонки~~
- ~~Если стори/тест поинты дробные, то они не округляются. В это сложно поверить, но кто-то может поставить 0.00000000000001 сп и верстка сломается. Поэтому стоит округлять до сотых~~
- ~~В проекте есть старые куски от будущего задела под cloud версию проекта под разные организации. Нужно понять стоит ли его оставлять на будущее чтобы можно было развивать или стоит выпилить~~ — **TRIM done**: tenant-модель org оставлена; вычищены SaaS stub'ы (`registerCloudUser`, switcher UI, dual SQL/tracker ветки, `isOnPremMode`).
- ~~Кнопки назад/вперед, есть ощущение что они просто не работают и не поодерживают новые режимы. Нужно посмотреть что они умеют делать и доработать их~~ — undo/redo: позиции + parent в «По фичам» + layout заметок/аннотаций

Новые фичи:

- ~~Добавить кнопку добавления нового спринта в селектор спринта. Нужно продумать по дизайну, есть вероятность что будет такое себе~~
- Продумать процесс онбординга. Пользак приходит и ничего непонятно сходу. Нужно сделать экраны с быстрым how to в коротких роликах
- ~~Добавить флоу добавления новых/существующих задач в спринт в сайдбаре в табе задач~~
- MCP / агентский контекст плана. Смысл есть не как прокси к трекеру (тикеты/статусы — у Tracker/Jira MCP), а как отдача уникального planning graph: раскладка по дням/сегментам, «по фичам» (lanes/draft parents), заметки/схемы, стрелки, goals, availability. Job: сторонний агент (Cursor/Claude) понимает что и в каком порядке делать в спринте.
  - v1 read-only: ресурсы/тулы на sprint/feature scope (positions, feature-lanes, notes(+текст схем), task_links, sprint_goals, absences). Сначала можно проверить спрос через один `GET …/sprint-context` JSON; MCP — тонкая обёртка, если экспорт реально используют.
  - v2 write (интересный сценарий): после исследования лид/PM даёт агенту самому спланировать декомпозицию фичи по плану — добавить заметки, создать/привязать задачи, проставить связи. Нужен approve в UI (или dry-run → apply), не silent writes. Без доверия к write MCP ценность сильно ниже.
- ~~Продуктовая проблема. Сейчас есть связь - одна доска - одна очередь/проект. При этом на это знание завязано по каким очередям/проектам тянется аналитика. А еще команды могут использовать одни и те же очереди~~ — разрешён шаринг одной очереди между командами (доска по-прежнему уникальна; синк дедупит queue keys). Multi-queue на одну команду — отдельно, если понадобится.
- ~~Нужно добавить синхронизацию дат начала, дедлайна с планером. Сейчас проект только пишет, но не синкает отображение по данным из трекера. По умолчанию дата начала = первый таймслот дня, а дедлайн последний таймслот дня, причем если у нас в бд есть данные по срокам, то они в приоритете~~
- ~~Раздел «Аналитика» (~~`/admin/analytics`~~) — не для конечного пользователя~~ — удалён UI/графики; ingest (`analytics_events`, `AnalyticsCollector`) оставлен для продуктовой аналитики
- Instance heartbeat (on-prem phone-home): анонимный суточный сигнал с инстанса наружу (`install_id`, version, tracker kind, грубые счётчики). Локальный `analytics_events` не уезжает. Opt-out. Дизайн — в чате / ниже по мере реализации.

